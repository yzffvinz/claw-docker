name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy
        run: |
          cd ~/claw-docker
          git pull
          docker compose pull
          docker compose up -d --remove-orphans
